kind: Ingress
metadata:
  name: good-ing
spec:
  defaultBackend:
    service:
      name: default-svc
      port:
        name: http
  rules:
    - host: example.com
      http:
        paths:
          - path: /
            backend:
              service:
                name: svc
                port:
                  name: http
